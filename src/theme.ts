import { createMuiTheme } from '@material-ui/core';

// @TODO this could probably go somewhere else eventually
export const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#375bd2',
    },
  },
});
